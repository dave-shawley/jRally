/**
 * Connectors to Agile project management tools.
 * This package is the basis for writing connectors to various Agile tools.
 * {@link standup.connector.ServerConnection} is the primary interface here.
 * It is responsible for translating whatever the backend provides into
 * elements in the {@link standup.xml} package.
 */
package standup.connector;
